version: 2
formats:
  - pdf
sphinx:
   configuration: docs/conf.py
python:
   version: "3.7"
   install:
   - requirements: requirements.txt
   extra_requirements:
     - tests
     - docs
conda:
  environment: environment.yml
